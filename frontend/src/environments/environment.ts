export const environment = {
  // production: false,
  production: true, // Use production flag when connecting to live backend? Maybe not, keep false for debugging but use live API.
  // apiUrl: 'http://localhost:3000/api/v1',
  apiUrl: 'https://accudocs.onrender.com/api/v1',
  appName: 'AccuDocs',
  appVersion: '1.0.0',
};
