
import { Injectable, signal, computed } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class DataTableService {
  // Shared logic for data tables if needed, e.g., global config or caching
  // Currently, most state will be instance-specific in the component
  constructor() { }
}
